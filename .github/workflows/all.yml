name: "Project's all the Workflows"

on:
  workflow_dispatch:
    # runs whenever a workflow has been dispatched
  push:
    branches: [master]

jobs:
  test_project:
    uses: ./.github/workflows/test_project.yml
    secrets:
      DJANGO_SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY }}
